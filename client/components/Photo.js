import React from 'react';

const Photo = React.createClass({
    render() {
        return (
            <figure className="photo-grid">
                I am a photo!
            </figure>
        )
    }
});

export default Photo;